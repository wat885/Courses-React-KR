// Arow function

fullname = (fname, lname) => `${fname} ${lname}`;
setAge = (age) => `อายุ ${age}`

// function fullname(fname,lname) {
//     return `${fname} ${lname}`;
// }

console.log(fullname("wat", "tas"));
console.log(setAge(222))
